// Area calculation for room #1




//we need to keep coding the same thing over and over. So we should consider DRY and make a function

func area() {
	let length = 10
	let width = 12
	
	let room = length * width
	print(room)
}

area()